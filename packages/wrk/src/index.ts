/**
 * Public entry point for `@macintacos/wrk`.
 *
 * The CLI itself is built out across the EXC-982 foundations track; this module
 * exists so the package's `exports` map resolves and the type-check program has
 * this package in scope.
 *
 * @packageDocumentation
 */

export {};
