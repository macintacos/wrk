/**
 * Public entry point for `@macintacos/wrk-picker`.
 *
 * The picker component lands in EXC-985; its public API surface and semver
 * policy are settled in EXC-1014, before the first publish. This module exists
 * so the package's `exports` map resolves and the type-check program has this
 * package in scope.
 *
 * @packageDocumentation
 */

export {};
